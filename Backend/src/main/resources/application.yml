spring:
  config:
    # Supports starting from the repository root or from Backend/.
    import: "optional:file:./Backend/.env[.properties],optional:file:./.env[.properties]"
  application:
    name: english-ai

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${POSTGRES_DB:mydatabase}
    username: ${POSTGRES_USER:myuser}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

security:
  jwt:
    # Base64-encoded random key containing at least 32 bytes. No default secret.
    secret: ${JWT_SECRET}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:900}
  refresh-token:
    # Base64-encoded random key containing at least 32 bytes. Required to issue refresh tokens.
    hash-secret: ${SECURITY_REFRESH_TOKEN_HASH_SECRET}
